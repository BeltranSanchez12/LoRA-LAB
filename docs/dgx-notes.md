# Notas del contenedor DGX

- **Identidad git** fijada a la cuenta de GitHub del proyecto
  (`BeltranSanchez12` / `202216017@alu.comillas.edu`) para que todos los commits
  futuros salgan a nombre correcto. Config `--global`; se eliminó un override
  `--local` de este repo que apuntaba a una cuenta personal. No se reescribió
  ningún commit pasado.
